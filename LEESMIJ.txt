v3.81.0 - alleen de gewijzigde bestanden sinds v3.80.0

CODE (naar Home Assistant, daarna herstarten)
  custom_components/energy_management_system/
    coordinator.py            de platformlijst uit PLATFORMS
    const.py                  PLATFORMS en de drempels
    __init__.py               importeert PLATFORMS uit const
    manifest.json             versienummer 3.81.0
    bestandscontrole.json     bijgewerkte hashlijst  (MOET mee)

TOETSEN (repository)
  tests/test_platformcontrole.py

DOCUMENTEN
  CHANGELOG.md  README.md  OVERDRACHT.md

WAT ER FOUT WAS
---------------

De platformcontrole uit v3.79.0 meldde twee omgevallen onderdelen:
`number` en `select`. Die bestaan helemaal niet in deze integratie - er
is geen number.py en geen select.py.

Vals alarm van mijn eigen makelij, en dezelfde fout als die de controle
moest vangen: iets schrijven op grond van wat ik DENK dat er hoort te
staan.

De lijst komt nu uit PLATFORMS, op een plek.

WAT JE ZELF MOET DOEN
---------------------

Kopieren en herstarten. Daarna staat de analyse weer op nul fouten.
