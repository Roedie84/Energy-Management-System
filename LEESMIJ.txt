EMS v4.2 - alleen de wijzigingen sinds v4.1.1

TE KOPIEREN naar config/custom_components/energy_management_system/

    const.py, coordinator.py, nabeschouwing.py
    manifest.json, bestandscontrole.json

NAAR DE REPO

    README.md, CHANGELOG.md, twee toetsbestanden

WAT ER VERANDERT

1. Een schrijver van de kaarttabel "diepste tekort".
2. De nabeschouwing splitst "gemist" in reserve en voorspelling.
3. De logregel over onverwachte netimport noemt de leesbare reden,
   het accuvermogen en de laadstand.
4. De proefstand wordt een keer per ronde gerekend; rekentijd per
   kandidaat in de export.

Na installatie: proefstand.rekentijd_ms - als daar iets ver boven de
100 ms staat, is dat de volgende.
